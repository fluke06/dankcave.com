// -----------------------------------------------------------------------------
// Dankcave theme JS. Vanilla, no jQuery.
// Populated as interactive components are built (mobile nav, cart drawer, etc.).
// -----------------------------------------------------------------------------

document.addEventListener( 'DOMContentLoaded', function () {
	// TODO: Mobile hamburger toggle
	// TODO: Cart drawer open/close
	// TODO: Product image gallery swap
	// TODO: Sticky header on scroll (if design calls for it)
} );
